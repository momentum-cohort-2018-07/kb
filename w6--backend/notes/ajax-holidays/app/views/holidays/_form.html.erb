<%= form_with(model: holiday, remote: true) do |form| %>
  <% if holiday.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(holiday.errors.count, "error") %> prohibited this holiday from being saved:</h2>

      <ul>
      <% holiday.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :observed_on %>
    <%= form.date_select :observed_on %>
  </div>

  <div class="field">
    <%= form.label :origin_country %>
    <%= form.text_field :origin_country %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
